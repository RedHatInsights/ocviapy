version: 2

updates:
# keep up to date the github actions
- package-ecosystem: github-actions
  directory: /
  schedule:
    interval: monthly
  groups:
    minor-actions-dependencies:
      update-types:
      - minor
      - patch
  commit-message:
    include: scope
    prefix: ci

# enable python dependencies security updates
- directory: /
  open-pull-requests-limit: 0
  package-ecosystem: pip
  rebase-strategy: auto
  schedule:
    interval: daily
  commit-message:
    include: scope
    prefix: chore
